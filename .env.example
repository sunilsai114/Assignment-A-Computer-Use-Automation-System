# Copy to .env and fill in. Never commit .env.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Sign-in for the LOCAL MOCK bank only. Synthetic values, also hard-coded in mock_app/data.py.
# Replay reads these at runtime; they are never written into an artifact or a log.
HERITAGE_USER=teller1
HERITAGE_PASS=demo-only
